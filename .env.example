# FanDuel Bet Tracker - Environment Variables Configuration
# Copy this file to .env and fill in your actual values

# Flask Configuration
SECRET_KEY=your-secret-key-here

# Google OAuth Configuration (Optional)
# To enable Google OAuth, you need to:
# 1. Go to https://console.developers.google.com/
# 2. Create a new project or select existing one
# 3. Enable Google+ API
# 4. Create OAuth 2.0 credentials
# 5. Add http://127.0.0.1:5000/callback/google to authorized redirect URIs
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Apple OAuth Configuration (Optional)
# To enable Apple OAuth, you need to:
# 1. Go to https://developer.apple.com/account/
# 2. Create an App ID and Services ID
# 3. Configure Sign in with Apple
# 4. Add http://127.0.0.1:5000/callback/apple to authorized redirect URIs
APPLE_CLIENT_ID=your-apple-client-id
APPLE_CLIENT_SECRET=your-apple-client-secret

# Database Configuration (Optional - defaults to SQLite)
# DATABASE_URL=sqlite:///bet_tracker.db

# File Upload Configuration (Optional)
# MAX_CONTENT_LENGTH=16777216  # 16MB in bytes
